# 要件定義

## 目的
処理件数が増えないバグの修正。

## 根本原因
article セレクタが X の現在の DOM と不一致。
- 現状: article[data-testid="article"]
- 正しい値: article[data-testid="tweet"]

シールドは表示されるが件数が増えない = ポストが0件検出のままスクロールし続ける状態。

## 成功基準
セレクタ修正後、処理件数が増えること（ポストが検出・削除されること）
