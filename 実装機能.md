# 実装仕様

① 凍結・bot判定対策（タイミングの完全ランダム化）
2〜8秒のランダム待機: 削除や取消アクションが発生するたびに、毎回 2000ms〜8000ms の間でランダムなミリ秒を算出して待機します。

小休憩・長休憩: 内部カウンターで処理件数を追跡し、50件到達時に120〜180秒（2〜3分）、200件到達時に600〜900秒（10〜15分） の休憩を自動挿入します。

500件での自動停止: カウンターが500に達すると、X側の1日あたりの規制ラインに接触するのを防ぐため、処理を完全にストップさせアラートを出します。

人間らしい操作（20%の確率）: 1ループ（タイムラインの走査）ごとに20%の確率でフラグが立ち、削除を行わずに「滑らかに少し下へスクロールし、数秒間静止した後に少し上へ戻る」という、タイムラインを閲覧している人間の挙動を完全にエミュレートします。

② 表示・安全設計
事前確認: 設定画面で「削除を開始する」を押すと、現在選択している削除対象と、入力したすべての保護条件（キーワード、いいね数、対象ID）を一覧化したブラウザネイティブのダイアログを表示し、誤りがないか再確認を求めます。

誤操作防止シールド: 実行が開始されると、画面全体に rgba(0,0,0,0.4) の半透明カバー（#x-del-shield）を展開します。これにより、処理中に背後のタイムラインを誤ってクリックしてしまうミスを物理的に防ぎます。

カウントダウンと進捗: シールド中央のステータスボードに現在の処理件数をリアルタイム表示し、各種休憩タイムに入った場合は「残り約 ○○ 秒」と1秒刻みでカウントダウンを行います。

画面スリープ防止: ブラウザの標準APIである navigator.wakeLock を実装しています。これにより、ツールを実行したままPCや端末を放置しても、処理中に画面が暗転したりスリープ状態（バックグラウンド移行によるスクリプトの停止）になるのを防ぎます。